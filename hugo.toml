baseURL = 'https://example.org/'
languageCode = 'en-us'
title = 'UD Basketball'
theme = "mainroad"

[Params]
  description = "UD Basketball" # Site description. Used in meta description
  copyright = "UD Basketball" # Footer copyright holder, otherwise will use site title
  # opengraph = true # Enable OpenGraph if true
  schema = true # Enable Schema
  twitter_cards = true # Enable Twitter Cards if true
  readmore = false # Show "Read more" button in list if true
  authorbox = false # Show authorbox at bottom of pages if true
  toc = false # Enable Table of Contents
  pager = false # Show pager navigation (prev/next links) at the bottom of pages if true
  # post_meta = ["author", "date", "categories", "translations"] # Order of post meta information
  mainSections = ["post", "blog", "news"] # Specify section pages to show on home page and the "Recent articles" widget
  dateformat = "2006-01-02" # Change the format of dates
  # mathjax = true # Enable MathJax
  # mathjaxPath = "https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.6/MathJax.js" # Specify MathJax path
  # mathjaxConfig = "TeX-AMS-MML_HTMLorMML" # Specify MathJax config
  googleFontsLink = "https://fonts.googleapis.com/css2?family=Graduate&display=swap" # Load Google Fonts
  customCSS = ["css/custom.css"] # Include custom CSS files
  customJS = ["js/custom.js"] # Include custom JS files

  # # DEPRECATED PARAMS
  # subtitle = "" # Deprecated in favor of .Site.Params.logo.subtitle
  # highlightColor = "" # Deprecated in favor of .Site.Params.style.vars.highlightColor

  [Params.style.vars]
  highlightColor = "#bb4537" # Override highlight color

  # Override font-family sets
  # Take care of different quotes OR escaping symbols in these params if necessary
  fontFamilyPrimary = "'Open Sans', Helvetica, Arial, sans-serif"
  # Secondary font-family set responsible for pre, code, kbd, and samp tags font
  fontFamilySecondary = "SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace"

  [Params.logo]
  image = "img/ud-gen-logo.png" # Logo image. Path relative to "static"
  title = "UD Basketball" # Logo title, otherwise will use site title
  subtitle = "Est. 2023" # Logo subtitle
  # when all odds, all statistics, and all mouths around you say you have no chance. You don’t even blink . The fire inside of you just burns even brighter.

  [Params.sidebar]
  home = "right" # Configure layout for home page
  list = "right"  # Configure layout for list pages
  single = false # Configure layout for single pages
  # Enable widgets in given order
  widgets = ["motto", "social", "recent", "categories", "taglist"]

  [Params.widgets]
  recent_num = 5 # Set the number of articles in the "Recent articles" widget
  categories_counter = false # Enable counter for each category in "Categories" widget
  tags_counter = false # Enable counter for each tag in "Tags" widget

  [Params.widgets.social]
  cached = false # activate cache if true
  # Enable parts of social widget
  # facebook = "username"
  # twitter = "username"
  instagram = "basketballud2023"
  # linkedin = "username"
  # telegram = "username"
  # github = "username"
  # gitlab = "username"
  # bitbucket = "username"
  # email = "info@udbasketball.net"
  email2 = "dawonw660@gmail.com"

  [Params.widgets.motto]
  motto_title = "Underdog Mentality"
	motto = "When all odds, all statistics, and all mouths around you say you have no chance.<br><br>You don’t even blink. The fire inside of you just burns even brighter."

  [privacy]
    [privacy.instagram]
      disable = false
      simple = false

##
# menu
##
[Menu]
[[menu.main]]
    name = 'Home'
    pageRef = '/'
    weight = 1
[[menu.main]]
    name = 'About'
    pageRef = '/about'
    weight = 10
[[menu.main]]
    name = 'Posts'
    pageRef = '/post'
    weight = 20
[[menu.main]]
    name = 'Merch'
    pageRef = '/merch'
    weight = 30
